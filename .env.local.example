# Local Ollama configuration — no API key needed.
# Defaults shown below match a standard local Ollama install.
# Only uncomment/change these if Ollama runs on a different host/port
# or you want to use a different model.

# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3.1